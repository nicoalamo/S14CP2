<p id="notice"><%= notice %></p>

<h1>Companies</h1>

<div class="row">
  <div class="col-md-6">

    <table class="table table-striped">
      <thead>
        <tr>
          <th>Name</th>
          <th>Nº Empleados</th>
          <th colspan="2"></th>
        </tr>
      </thead>

      <tbody>
        <% @companies.each do |company| %>
          <tr>
            <td><%= link_to company.name, company %></td>
            <td><%= link_to company.employees.count, company %></td>
            <td><%= link_to 'Edit', edit_company_path(company), class:"btn btn-primary" %></td>
            <td><%= link_to 'Destroy', company, class:"btn btn-danger", method: :delete, data: { confirm: 'Are you sure?' } %></td>
          </tr>
        <% end %>
      </tbody>
    </table>

  </div>
</div>

<br>

<%= link_to 'New Company', new_company_path %>
